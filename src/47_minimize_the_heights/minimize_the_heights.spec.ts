import { minimizeTheHeights } from "./minimize_the_heights"

describe('MinimizeHeights', () => {
  it('works', () => {
    expect(0).toEqual(0)
  })
  // expect(minimizeTheHeights([1,5,8,10],2)).toEqual(5)
})
