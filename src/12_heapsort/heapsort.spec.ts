import { makeHeap, get, heapSort } from './heapsort'

describe('heapsort', () => {
  it('sorts correctly', () => {
    // expect(heapSort([7,3,1,4,6,5,8, 9])).toEqual([1,3,4,5,6,7,8,9])
    // expect(heapSort([1,4,2,3,5,6,7])).toEqual([1,2,3,4,5,6])
  })
})
